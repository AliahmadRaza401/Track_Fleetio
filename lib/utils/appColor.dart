import 'package:flutter/material.dart';

class AppColors {
  static const white = Color(0xffffffff);
  static const grey = Color(0xff999999);
  static const greyLight = Color(0xffD1D3D4);
  static const balck = Colors.black;
  static const noColor = Colors.transparent;
  static const primaryLight = Color(0xFFBE5161);

  static const primaryDark = Color(0xff993535);
static const nrcolor = Color(0xFFFF6A05);
static const offcolor = Color(0xFF212121);
static const pcolor = Color(0xFFE41F13);
static const icolor = Color(0xFFE7E720);
static const mcolor = Color(0xFF33A532);
 static const darkblue = Color(0xFF182451);



  static const purplegradient = LinearGradient(
    colors: [Color(0xFFBE5161), Color(0xFF993535)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );

  static const primarygradient = LinearGradient(
    colors: [Color(0xFFBE5161), Color(0xFF993535)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );

  static const totalUnitGradi = LinearGradient(
    colors: [Color(0xFF4D6F8C), Color(0xFF204B70)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );

  static const onlineGradi = LinearGradient(
    colors: [Color(0xFF67BC67), Color(0xFF33A532)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );

  static const offlineGradi = LinearGradient(
    colors: [Color(0xFFD46676), Color(0xFFC8354A)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );
  static const movingGradi = LinearGradient(
    colors: [Color(0xFF67BC67), Color(0xFF33A532)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );
  static const idleGradi = LinearGradient(
    colors: [Color(0xFFCCC16F), Color(0xFFE4D354)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );
  static const stopGradi = LinearGradient(
    colors: [Color(0xFF68B3A4), Color(0xFF1AA085)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );
  static const parkedGradi = LinearGradient(
    colors: [Color(0xFFE97777), Color(0xFFF45B5B)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );
  static const towingGradi = LinearGradient(
    colors: [Color(0xFFE97777), Color(0xFFF45B5B)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );
  static const notResponceGradi = LinearGradient(
    colors: [Color(0xFF566374), Color(0xFF566374)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );
}
